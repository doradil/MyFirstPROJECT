// trigger CaseChangeOwnerTrigger on Case (before insert) {
// for(case each :trigger.new) {
//    if (each.Status =='New'||each.Status=='Working') {
//     each.OwnerId=UserInfo.getUserid();
//    }
//    }

// }
//on the account object create a new field called total cases and create a trigger 
//on case so if the case is associated to 
//any account the total cases field should be recalculated



